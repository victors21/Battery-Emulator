#ifndef _UTILS_H_
#define _UTILS_H_

#include "debug_handler.h"
#include "events.h"
#include "timer.h"
#include "watchdog.h"

enum bms_status { STANDBY = 0, INACTIVE = 1, DARKSTART = 2, ACTIVE = 3, FAULT = 4, UPDATING = 5 };

#endif
